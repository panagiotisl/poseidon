<li>
  <%= link_to affiliation.name, affiliation %>
  <% if current_user.admin? %>
    | <%= link_to "delete", affiliation, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>
