<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "poseidon", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
              
          <li>
<form class="form-search navbar-form navbar-left" role="search" accept-charset="UTF-8" action="/search" method="get">
    <div class="input-append">
        <input id="term" name="term" type="text" class="span2 search-query">
        <button type="submit" class="btn"><span class="icon icon-search"></span></button>
    </div>
</form>
          </li>
          <% if signed_in? %>
			<li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %><b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
			<li id="fat-menu" class="dropdown span2">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
				<span class="icon icon-envelope"></span> <%= get_unread(get_mailbox.receipts) %> Mailbox<b class="caret"></b>
		      	<% if false %>
				
				<table border="0" cellpadding="0" cellspacing="0">
					<tr>
						<td width="10" height="20" align="center" valign="top" background="<%= image_path("messages2.png")%>" style="background-repeat:no-repeat;">
							<font size="+2.4" color="red" style="text-shadow: -2px 0 gray, 0 2px gray, 1px 0 gray, 0 -1px gray;"><%= get_unread(get_mailbox.receipts) %></font>
						</td>
						<td>Mailbox <b class="caret"></b></td>
					</tr>
				</table>
				
				
				<div class="image">
			    	<img src="<%= image_path("messages.png")%>" alt="" height="20" width="26"/><h2>2</h2>
				</div>
        
                <span class="icon icon-envelope"></span> <%= get_unread(get_mailbox.receipts) %> Messages<b class="caret"></b>
				<% end %>
              </a>
              <ul class="dropdown-menu">
				<li><%= link_to "New Message", new_message_path %></li>
				<li class="divider"></li>
                <li><%= link_to "Messages", conversations_path %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
