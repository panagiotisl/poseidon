<ul class="nav nav-pills">
	<% if params[:controller]=="messages" and params[:action]=="new" %>
    <li class="active">
    <% else %>
	<li>
	<% end %>
        <%= link_to "New Message", new_message_path %>
    </li>
	<% if params[:controller]=="conversations" and params[:action]=="index" %>
    <li class="active">
    <% else %>
	<li>
	<% end %>
        <%= link_to "Messages", conversations_path %>
    </li>
	<% if false %>
	<% if params[:box].eql? "sentbox" %>
    <li class="active">
    <% else %>
	<li>
	<% end %>
        <%= link_to "Sentbox", conversations_path(:box=>'sentbox') %>
    </li>
	<% if params[:box].eql? "trash" %>
    <li class="active">
    <% else %>
	<li>
	<% end %>
        <%= link_to "Thrash", conversations_path(:box=>'trash') %>
    </li>
	<% end %>
</ul>
