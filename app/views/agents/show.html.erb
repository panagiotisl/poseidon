<% provide(:title, @agent.name) %>
<div class="row">
	<div class="span12">
    	<%= render "nav" %>
	</div>
    <div class="span3 roundBox">
    	<% @voyages = @agent.voyages.paginate(page: params[:page]) %> 
       	  <h4>Voyages</h4>
    	  <%= will_paginate @voyages %>
            <ul class="voyages">
              <%= render @voyages %>
            </ul>
          <%= will_paginate @voyages %>
    </div>
</div>
