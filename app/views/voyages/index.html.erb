<% provide(:title, @title) %>
<%= render "/shared/nav" %>
<div class="row">
  <div class="roundBox">
    <%= will_paginate %>
    <table class="table-ships table-hover table-stripped">
      <tbody>
      <%= render @voyages %>
      </tbody>
    </table>
    <%= will_paginate %>
  </div>
</div>
