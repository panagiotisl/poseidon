<% content_for :title do %>
  <%=t('message.other')%>
<% end %>

<section id="new_message">

	<div class=roundBox>
  		<%= render :partial => 'index' %>

  		<%= render :partial => 'new' %>

	</div>
</section>
