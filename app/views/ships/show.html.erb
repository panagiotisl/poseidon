<% provide(:title, @ship.name) %>
<div class="row">
  <div class="span12">
  <%= render "nav" %>
     
  	Details of ship TBA
    <div class="voyages_list roundBox">
      <h4>Voyages</h4>
      <%= will_paginate @voyages %>
      <ul class="voyages">
        <%= render @voyages %>
      </ul>
      <%= will_paginate @voyages %>
    </div>
  </div>
</div>
