<% provide(:title, @ship.name) %>
<div class="row">
  <%= render "shipping_companies/nav" %>
  <div class="span8">
  	<%= link_to @shipping_company.name, shipping_company_path(:id => @shipping_company) %>:
  	<%= link_to @fleet.name, shipping_company_fleet_path(:id => @fleet) %>
  	<h3><%= @ship.name %></h3>
  	
  	Details of ship TBA<br />
  	
  	<%= link_to "Add voyage", new_shipping_company_fleet_ship_voyage_path(:ship_id => params[:id], :shipping_company_id => params[:shipping_company_id]) %>
  	  <div class="voyages_list">
  	  	<h5><%= "Voyages of #{@ship.name}"  %></h5>
        <%= will_paginate @voyages %>
        <ul class="voyages">
          <%= render @voyages %>
        </ul>
        <%= will_paginate @voyages %>
      </div>
  </div>
</div>
